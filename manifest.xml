<?xml version="1.0"?>
<!--
  Generated for v0. Two things to verify against your local SDK:
    1. The product id below. Open the SDK's device reference (or the VS Code
       device picker) and confirm "epix2pro47mm" is the id for epix Pro (Gen 2)
       47 mm. If it differs, change it here - nothing else depends on it.
    2. minApiLevel. 3.3.0 is the floor for glance support. Raise it only if a
       later API is actually needed.
-->
<iq:manifest xmlns:iq="http://www.garmin.com/xml/connectiq" version="3">
    <iq:application
        entry="UvGuardApp"
        id="fbc12f97fcf9479688c15a7647130c95"
        launcherIcon="@Drawables.LauncherIcon"
        minApiLevel="3.3.0"
        name="@Strings.AppName"
        type="watch-app">

        <iq:products>
            <iq:product id="epix2pro47mm"/>
        </iq:products>

        <iq:permissions>
            <iq:uses-permission id="Communications"/>
            <iq:uses-permission id="Positioning"/>
            <iq:uses-permission id="Sensor"/>
        </iq:permissions>

        <iq:languages>
            <iq:language>eng</iq:language>
        </iq:languages>

        <iq:barrels/>
    </iq:application>
</iq:manifest>
